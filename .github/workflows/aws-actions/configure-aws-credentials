name: Deploy to AWS

on:
  push:
    branches:
      - main

permissions:
  id-token: write   # Required to request the OIDC JWT from GitHub
  contents: read    # Required to check out code

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::<ACCOUNT_ID>:role/<ROLE_NAME>
          aws-region: <YOUR_AWS_REGION>   # e.g., us-east-1, ap-southeast-1
          # Optional:
          # role-session-name: GitHubActions-${{ github.run_id }}
          # audience: sts.amazonaws.com   # Defaults to sts.amazonaws.com

      - name: Verify AWS Connection
        run: |
          aws sts get-caller-identity
          echo "Assumed Role ARN:"
          aws sts get-caller-identity --query "Arn" --output text

          echo "Configured AWS Region:"
          aws configure get region
